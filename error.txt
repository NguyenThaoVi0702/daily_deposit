SELECT
	DISTINCT conversion_date,
	from_currency,
	CAST(conversion_rate AS double) AS conversion_rate
FROM
	ogl.ogl_gl_daily_rates
WHERE
	conversion_date >= '2024-12-09'
	AND conversion_type = '1009'
	AND to_currency = 'VND'
